# Algorithm
"""
	1) Sort all the Edges according to their weight
	2) choose this eges one by one . If selected edges make a cycle live that Edge
	3) do until all edges are finished

	Complexity = O(n log(n)) + n 
	           = sort + loop throught all edges

"""